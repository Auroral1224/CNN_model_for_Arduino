要修正部分：
Pruning後的Latency需重新測量

新功能：
丟入ESP32中實行
量化(兩部分)：訓練前Datatype從float32轉成int8 ??(QAT)    or   訓練後量化(V)(Dynamic range quantization)
比對Accuracy及Latency製成圖表

tflite->h: git bash here:
xxd -i model.tflite > model_data.h

Post-training quantization
Quantization Aware Training
Pruning

----

(All tflite)

(1) Baseline model
(2) Non pruned, but quantized
(3) Pruned model, not quantized
(4) Pruned and quantized model
(5) Pruned and QATed